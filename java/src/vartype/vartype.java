package vartype;

import static java.lang.System.out;
import static vartype.lib.len;

public class vartype {
    public static void main(String[] args) {
        String strings = "abcdef";
        Integer a = 54;

        out.print(len(a));
    }
}
